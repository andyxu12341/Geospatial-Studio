{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": false
  },
  "npm": {
    "publish": true
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "hooks": {
    "after:bump": "node -e 'const fs = require(\"fs\"); const pkg = JSON.parse(fs.readFileSync(\"package.json\", \"utf8\")); fs.writeFileSync(\"README.md\", fs.readFileSync(\"README.md\", \"utf8\").replace(/version-v[0-9]+\\.[0-9]+\\.[0-9]+/g, \"version-v\" + pkg.version))' && git add README.md CHANGELOG.md"
  }
}
